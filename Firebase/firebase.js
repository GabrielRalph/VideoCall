import {getUser, getUID, addAuthChangeListener, getDB} from "./firebase-basic.js"
import * as RTCSignaler from "./rtc-signaler.js"

// await new Promise(function(resolve, reject) {
//   setTimeout(resolve, 2000)
// });
export {getUser, getUID, addAuthChangeListener, getDB, RTCSignaler}
